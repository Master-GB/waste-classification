model:
  name: efficientnetb0
  pretrained: true
  num_classes: 10
  freeze_backbone: true

training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.001
  weight_decay: 0.0001

optimizer:
  name: AdamW

experiment:
  name: efficientnetb0_frozen_backbone
  seed: 42
